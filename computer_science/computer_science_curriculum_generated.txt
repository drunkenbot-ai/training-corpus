A variable stores a value.
Example: x = 5.
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A loop repeats steps.
Example: for item in items.
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A function groups steps.
Example: def add(a, b).
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A algorithm is a set of steps.
Example: sort the numbers.
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A conditional chooses a path based on a check.
Example: if score > 50.
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A class groups data and behavior.
Example: class Counter.
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A array index points to one item's position.
Example: items[0].
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A boolean is either true or false.
Example: is_ready = True.
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A string stores text.
Example: name = 'Mina'.
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What did you notice?

A variable stores a value.
Example: x = 5.
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A loop repeats steps.
Example: for item in items.
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A function groups steps.
Example: def add(a, b).
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A algorithm is a set of steps.
Example: sort the numbers.
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A conditional chooses a path based on a check.
Example: if score > 50.
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A class groups data and behavior.
Example: class Counter.
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A array index points to one item's position.
Example: items[0].
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A boolean is either true or false.
Example: is_ready = True.
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A string stores text.
Example: name = 'Mina'.
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Why do you think that happens?

A variable stores a value.
Example: x = 5.
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A loop repeats steps.
Example: for item in items.
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A function groups steps.
Example: def add(a, b).
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A algorithm is a set of steps.
Example: sort the numbers.
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A conditional chooses a path based on a check.
Example: if score > 50.
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A class groups data and behavior.
Example: class Counter.
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A array index points to one item's position.
Example: items[0].
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A boolean is either true or false.
Example: is_ready = True.
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A string stores text.
Example: name = 'Mina'.
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
How could you check this?

A variable stores a value.
Example: x = 5.
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A loop repeats steps.
Example: for item in items.
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A function groups steps.
Example: def add(a, b).
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A list keeps items in order.
Example: numbers = [1, 2, 3].
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A algorithm is a set of steps.
Example: sort the numbers.
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A conditional chooses a path based on a check.
Example: if score > 50.
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A class groups data and behavior.
Example: class Counter.
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A array index points to one item's position.
Example: items[0].
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A boolean is either true or false.
Example: is_ready = True.
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A string stores text.
Example: name = 'Mina'.
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
What would change if one detail changed?

A variable stores a value.
Example: x = 5.
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Mina reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Ravi reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Lena reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Omar reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Sara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Tara reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Jin reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Ada reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Leo reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Nia reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Sam reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Priya reads the code carefully.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Mina reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Ravi reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Lena reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Omar reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Sara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Tara reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Jin reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Ada reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Leo reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Nia reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Sam reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Priya reads the code quickly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Mina reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Ravi reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Lena reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Omar reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Sara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Tara reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Jin reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Ada reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Leo reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Nia reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Sam reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Priya reads the code calmly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Mina reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Ravi reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Lena reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Omar reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Sara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Tara reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Jin reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Ada reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Leo reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Nia reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Sam reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Priya reads the code clearly.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Mina reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Ravi reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Lena reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Omar reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Sara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Tara reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Jin reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Ada reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Leo reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Nia reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Sam reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A variable stores a value.
Example: x = 5.
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A loop repeats steps.
Example: for item in items.
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A function groups steps.
Example: def add(a, b).
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A list keeps items in order.
Example: numbers = [1, 2, 3].
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A dictionary connects keys to values.
Example: scores = {'Mina': 9}.
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A algorithm is a set of steps.
Example: sort the numbers.
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A conditional chooses a path based on a check.
Example: if score > 50.
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A recursion calls itself on a smaller case.
Example: factorial(n - 1).
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A class groups data and behavior.
Example: class Counter.
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A array index points to one item's position.
Example: items[0].
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A boolean is either true or false.
Example: is_ready = True.
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.

A string stores text.
Example: name = 'Mina'.
Priya reads the code patiently.
A programmer reads errors carefully.
Debugging means finding the cause.
Testing checks if code works.
Small steps make hard problems easier.
Explain it back in your own words.
